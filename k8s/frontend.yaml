apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: sonbarse17/frontend:latest
        ports:
        - containerPort: 8080
        securityContext:
          runAsNonRoot: true
          allowPrivilegeEscalation: false
          # Secure: Prevent malicious writes to application filesystem
          readOnlyRootFilesystem: true
          # Secure: Minimize attack surface by dropping ALL default kernel capabilities
          capabilities:
            drop:
              - ALL
        resources:
          limits:
            cpu: "250m"
            memory: "256Mi"
          requests:
            cpu: "100m"
            memory: "128Mi"
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 5
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 5
        env:
        - name: BACKEND_URL
          value: "http://backend-service:5000"
        # Secure: added probes, resource limits, securityContext, and unprivileged port
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  # Secure: ClusterIP instead of NodePort
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: frontend
